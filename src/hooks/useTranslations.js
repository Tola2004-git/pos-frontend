// Re-exported from a shared Context (see context/LanguageContext.jsx) - kept
// at this path so every existing `import { useTranslations } from
// "../hooks/useTranslations"` call site doesn't need to change.
export { useTranslations } from "../context/LanguageContext";
